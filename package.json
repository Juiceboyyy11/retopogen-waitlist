{
  "name": "waitlist-site",
  "version": "1.0.0",
  "description": "Beautiful waitlist landing page with admin dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}